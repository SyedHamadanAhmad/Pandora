You are generating **one React component** for a design-system POC.

## Component spec (JSON)

{{ spec_json }}

## Design tokens (JSON)

{{ design_tokens_json }}

## Typography (JSON)

{{ typography_json }}

## Spacing system (JSON)

{{ spacing_json }}

## Global config (JSON)

{{ global_config_json }}

{% if revision_instruction %}
## Revision instruction

{{ revision_instruction }}
{% endif %}

---

## Type-specific markup (spec type: {{ spec_type }})

{% if spec_type == 'button' %}
- Label-only API: required `label: string` and `onClick?: () => void` — do not use `children`.
- Render `<button type="button" onClick={onClick}>{label}</button>`.
- Variants must differ using design tokens; include hover, focus-visible, disabled in CSS.
{% elif spec_type == 'card' %}
- Root: `<article>` with header + body; border/shadow from tokens.
{% elif spec_type == 'badge' %}
- Root: `<span>` (inline); compact padding; token-based colors.
{% elif spec_type == 'navigation' %}
- `<nav>` with `<ul>` / `<li>`; active link state.
{% elif spec_type == 'input' %}
- `<label htmlFor>` + `<input id>`.
{% elif spec_type == 'modal' %}
- `role="dialog"`, labelled title, overlay + panel.
{% elif spec_type == 'hero' or spec_type == 'layout' %}
- `<section>` and heading hierarchy.
{% else %}
- Semantic HTML for type "{{ spec_type }}" and {{ component_name }}.
{% endif %}

{% include 'component_api_contract.jinja2' %}

{% include 'design_quality_contract.jinja2' %}

---

Return a **single JSON object** with **exactly** these keys:

- `tsx_code` (string, required): React 18 + TypeScript. `export function {{ component_name }}(...)`. Import `react` for `ReactNode` when needed. `className` prefix `pandora-{{ component_name_lower }}`.
- `css_code` (string or null): Scoped CSS.
- `props` (object): Demo values for **all required** API props (e.g. `label`, `text`, `title` strings for previews; omit `onClick` or use null).
- `variants` (array of strings): From spec or `["default"]`.

Do not include keys other than the four above.
